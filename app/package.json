{
  "name": "app",
  "version": "1.0.0",
  "description": "Application to interface with the Sentinels",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "watch": "nodemon --exec electron .",
    "reset": "git reset --hard",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^25.1.1",
    "nodemon": "^2.0.22"
  },
  "dependencies": {
    "bootstrap": "^5.3.0"
  }
}
